esphome:
  name: matrix-display

esp32:
  board: esp32dev

external_components:
  - source:
      type: local
      path: ../components

display:
  - platform: fpga_matrix_display
    id: matrix
    width: 64
    height: 32
